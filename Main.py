import requests
import re
import TravelVars as tv
import json
import Creds






# make_all_combo_sessions()
# for key in keys:
#     poll_session(key)
#
# for price in prices:
#     print(price)

print(json.loads("Creds.json")["host"])

